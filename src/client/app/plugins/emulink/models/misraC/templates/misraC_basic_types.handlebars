{{#if template_description}}
%
%   Description: This template generates type declarations for basic types (bool, int, real, etc).
%
{{/if}}/**
 * Basic type declarations
 */


typedef {{char_t}} char_t;
#ifndef _GCC_WRAP_STDINT_H
typedef {{int8_t}} int8_t;
typedef {{int16_t}} int16_t;
typedef {{int32_t}} int32_t;
typedef {{int64_t}} int64_t;
typedef {{uint8_t}} uint8_t;
typedef {{uint16_t}} uint16_t;
typedef {{uint32_t}} uint32_t;
typedef {{uint64_t}} uint64_t;
#endif
typedef {{float32_t}} float32_t;
typedef {{float64_t}} float64_t;
typedef {{float128_t}} float128_t;

// typedef bool uint8_t;
typedef int bool;
typedef char* string;

#define true 1
#define false 0
#define TRUE 1
#define FALSE 0
